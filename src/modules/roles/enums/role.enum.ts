export enum EnumRoleIds {
  PATIENT = 1,
  RECEPTIONIST = 2,
  MANAGER = 3,
  DOCTOR = 4,
  ADMIN = 5,
}

export enum EnumRoleValues {
  PATIENT = 1,
  RECEPTIONIST = 4,
  MANAGER = 16,
  DOCTOR = 64,
  ADMIN = 256,
}
